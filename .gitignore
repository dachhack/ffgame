node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log
npm-debug.log*

*.tsbuildinfo

# Build-time PBP dumps from the Stathead MCP. scripts/pbp/raw/ (per-game full
# play-by-play) IS committed — it's the canonical source genRealPbp.mjs +
# genReturns.mjs bake from. rtdump/ is the legacy lightweight dump, not needed.
scripts/pbp/rtdump/
# Raw ESPN summary responses cached by genGameFeed.mjs (refetchable, big).
scripts/pbp/espn-cache/

# Supabase CLI local scratch (linked project ref, version cache) — not for VCS.
supabase/.temp/
