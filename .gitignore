node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log
npm-debug.log*

*.tsbuildinfo

# Build-time PBP dumps pulled from the Stathead MCP and consumed by the
# generators in scripts/pbp/. Regenerable (re-pull) and not shipped — the baked
# output lives in public/pbp/wN.json + src/data/returns.ts.
#   raw/    — per-game full play-by-play (genRealPbp.mjs + genReturns.mjs)
#   rtdump/ — legacy lightweight time_of_day dumps
scripts/pbp/raw/
scripts/pbp/rtdump/
